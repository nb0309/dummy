{
  "name": "wcag-sr-dataset-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scalable Playwright + axe-core + @guidepup/virtual-screen-reader capture pipeline: turns GDS WCAG audit HTML files (1.1.1 / 1.2.1 / 1.3.1) into a per-element labeled dataset matching dataset.csv's schema.",
  "scripts": {
    "capture": "node capture.mjs",
    "dryrun": "node capture.mjs --no-sr"
  },
  "dependencies": {
    "@guidepup/virtual-screen-reader": "^0.32.1",
    "@playwright/test": "^1.57.0",
    "axe-core": "^4.10.2"
  }
}
